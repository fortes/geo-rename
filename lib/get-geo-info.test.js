// TODO: Mock lock functions (create a static `__mock__`?)

const {getGeoInfo} = require('./get-geo-info');

// TODO: Create mock cache

test('extracts latlng from exif', () => {});

test('does not query google when offline', () => {});
